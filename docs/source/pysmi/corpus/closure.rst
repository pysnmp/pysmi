.. _corpus.closure:

Import closures
---------------

Which files a consumer needs in order to load a module. ``core.db``'s
``import`` table holds the direct edges; this is the closure over them, which
is the question consumers actually arrive with. See :ref:`import-closure` for
the artifact a build writes.

.. automodule:: pysmi.corpus.closure
  :members:
